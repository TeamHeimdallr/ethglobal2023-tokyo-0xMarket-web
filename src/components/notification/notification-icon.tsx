import tw from 'twin.macro';

import { ButtonIconLarge } from '../buttons';
import { IconNoti } from '../icons';

export const NotificationIcon = () => {
  return <ButtonIconLarge icon={<IconNoti />} />;
};
